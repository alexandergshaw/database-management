-- Week 0 — Catalog settings.
-- Fill in each TODO, then run this WHOLE file in the Supabase SQL editor.
-- The line below clears the table first, so you can safely run this again
-- whenever you want to start over.

drop table if exists catalog_settings cascade;

-- Problem 1 — Create a table named "catalog_settings" with these columns:
--    id            a unique id the database fills in for you (the primary key)
--    catalog_name  the name of your catalog/observatory  (text, required)
--    tagline       a short slogan                         (text, required)
--    created_at    when the row was created               (timestamp, defaults to now)
-- The worked example in INSTRUCTIONS.md shows exactly this CREATE TABLE shape.
-- TODO: write your CREATE TABLE statement below.



-- Problem 2 — Add ONE row containing your own catalog name and tagline.
-- (This is an INSERT — see the example in INSTRUCTIONS.md.)
-- TODO: write your INSERT statement below.


