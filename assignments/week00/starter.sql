-- Week 0 — Catalog settings.
-- Fill in each TODO, then run the whole script in the Supabase SQL editor.

drop table if exists catalog_settings cascade;

-- Problem 1 — create the catalog_settings table:
--   id           uuid, primary key, default gen_random_uuid()
--   catalog_name text, not null
--   tagline      text, not null
--   created_at   timestamptz, not null, default now()
-- TODO:


-- Problem 2 — insert ONE row with your observatory's name and tagline.
-- TODO:
