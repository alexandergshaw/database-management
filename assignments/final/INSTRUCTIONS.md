# Week 16 — Review 3, Test 3 & Final Presentation

The capstone. These views span weeks 13–15 (transactions, security, analytics),
and the fully assembled catalog is your demo.

## Problems (in `assignments/final/starter.sql`)
1. **Analytics** — `final_type_summary` (planets and avg radius per type).
2. **Joins** — `final_moon_leaders` (planets ranked by moon count).
3. **Transactions** — `final_booking_log` (what `book_nights` recorded, from the
   Week 13 telescope tables).
4. **Security** — `final_public_listing` (reads the Week 14 `public_catalog`).

## Done when
- The Week 16 planet is **Unlocked** (all four views exist).

## Presentation checklist
- Walk through the homepage and name the SQL behind each section.
- Explain one design decision (a junction table, a normalization split, the
  transaction function, or an RLS policy).

---

**Retry anytime:** re-run the script (`create or replace view`).
