# Week 9 — Test 2

Assessed views covering everything from Review 2 (weeks 5–7).

## Problems (in `assignments/test02/starter.sql`)
1. **Normalization** — `test2_star_body_counts` (bodies per star, 3NF).
2. **CRUD / anti-join** — `test2_never_observed` (planets never observed).
3. **Filtering + sorting** — `test2_cold_planets` (coldest planets).

## Done when
- The Week 9 planet is **Unlocked** (all three views exist).

---

**Retry anytime:** re-run the script (`create or replace view`).
