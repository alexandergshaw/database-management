# Week 7 — Test 1

Assessed views over the design from weeks 1–5.

## Your SQL task
Run `assignments/test1/solution.sql`, which creates two views:
- `test1_products_per_category` — products in each category (including empty
  categories).
- `test1_inventory_value` — total value of inventory on hand (price × stock).

## Done when
- The Week 7 planet is **Unlocked** (both views exist).

---

**Retry anytime:** re-run the script (`create or replace view`). Remove with
`drop view if exists test1_products_per_category, test1_inventory_value;`.
