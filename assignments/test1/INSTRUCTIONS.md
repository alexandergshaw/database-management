# Week 7 — Test 1

Assessed views covering everything from Review 1 (weeks 1–5).

## Problems (in `assignments/test1/starter.sql`)
1. **Many-to-many** — `test1_products_per_category` (products in each category).
2. **Aggregation** — `test1_inventory_value` (total price × stock).
3. **Data modeling** — `test1_suppliers_by_country` (suppliers per country).
4. **Normalization** — `test1_brand_product_counts` (products per brand from the
   3NF tables).

## Done when
- The Week 7 planet is **Unlocked** (all four views exist).

---

**Retry anytime:** re-run the script (`create or replace view`).
