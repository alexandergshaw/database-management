-- Week 10 — Joins & multi-table queries.

-- Problem 1 — create a view named order_history with columns id, status,
-- created_at, customer (full_name), item_count (sum of quantities), total (sum
-- of quantity * unit_price). Join orders -> customers and orders -> order_items,
-- group by the order, newest first.
-- TODO:


-- Problem 2 — create a view named customer_order_counts with columns customer
-- and orders (number of orders). Use a LEFT JOIN so customers with zero orders
-- still appear with 0.
-- TODO:
