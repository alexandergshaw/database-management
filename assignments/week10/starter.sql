-- Week 10 — Joins & multi-table queries.

-- Problem 1 — create a view named observation_log with columns id, observed_at,
-- status, astronomer (full_name), planet (name), magnitude. Join observations ->
-- astronomers and observations -> planets, newest first.
-- TODO:


-- Problem 2 — create a view named planet_observation_counts with columns planet
-- and observations (count). Use a LEFT JOIN so never-observed planets show 0.
-- TODO:
