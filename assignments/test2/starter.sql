-- Test 2 — assessed views over the order data (weeks 8-10).

-- Problem 1 (joins + aggregation) — test2_spend_by_customer: customer, spend
-- (sum of quantity * unit_price), highest first.
-- TODO:


-- Problem 2 (filtering / anti-join) — test2_never_ordered: name of every product
-- with no order_items. Hint: `where not exists (...)`.
-- TODO:


-- Problem 3 (joins + aggregation + sorting) — test2_orders_by_total: id, total
-- (sum of quantity * unit_price) per order, biggest first.
-- TODO:
