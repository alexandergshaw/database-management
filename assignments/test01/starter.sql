-- Test 1 — assessed views across weeks 1-2. Create three views.
-- A view looks like:  create or replace view NAME as SELECT ... ;

-- Problem 1 — Create a view named "test1_planets_per_type" with:
--    type     — the planet type
--    planets  — how many planets are that type
-- Hint: GROUP BY type and count.
-- TODO:


-- Problem 2 — Create a view named "test1_avg_radius" with one column:
--    avg_radius_km — the average of every planet's radius_km, rounded to 1 decimal
-- Hint: use avg(...) and round(..., 1). No GROUP BY needed (it's one number).
-- TODO:


-- Problem 3 — Create a view named "test1_missions_by_agency" with:
--    agency   — the space agency
--    missions — how many missions that agency has
-- Hint: GROUP BY agency and count.
-- TODO:
